{
  "name": "client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npx serve . -l 5173",
    "build": "mkdir -p dist && cp -r src dist/ && cp index.html dist/"
  }
}